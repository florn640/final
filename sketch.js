function setup() {
	// create a place to draw
	createCanvas(1270, 630);
}

function draw() {
	// clear the background
	background(0, 0, 0);

	// set a fill color
	fill(255, 255, 100);


	// draw a circle
	ellipse(320, 180, 100, 100);
}
